@model IEnumerable<PicturesModel>

    <div class="card shadow-sm r-gallery-sec">
        <div class="card-header r-gallery-title">
            Photo Gallery
        </div>
        <div class="card-body r-gallery-body">
            <div class="row">
                <div id="r-gallery-carousel" class="carousel slide w-100" data-ride="carousel">
                    <div class="carousel-inner w-100" role="listbox">
                        @{ int i = 0;}
                        @foreach (var item in Model)
                        {
                            if (i == 0)
                            {<div class="carousel-item active">
                                    <div class="col-md-3">
                                        <img src="@item.pictureurl" class="img-thumbnail" alt="@item.albumid">
                                    </div>
                                </div>}
                            else
                            { <div class="carousel-item">
                                    <div class="col-md-3">
                                        <img src="@item.pictureurl" class="img-thumbnail" alt="@item.albumid">
                                    </div>
                                </div>
                            }
                            i = i + 1;

                        }
                    </div>
                </div>
            </div>
            <div class="r-gallery-controls mt-3 text-right">
                <a href="#r-gallery-carousel" class="btn btn-secondary" data-slide="prev"><i class="fa fa-chevron-left"></i></a>
                <a href="#r-gallery-carousel" class="btn btn-secondary" data-slide="next"><i class="fa fa-chevron-right"></i></a>
            </div>
        </div>
    </div>

    <script src="//ajax.googleapis.com/ajax/libs/jquery/1.7.1/jquery.min.js" type="text/javascript"></script>
    
    <script type="text/javascript">
        $('.carousel .carousel-item').each(function () {
            var minPerSlide = 4;
            var next = $(this).next();
            if (!next.length) {
                next = $(this).siblings(':first');
            }
            next.children(':first-child').clone().appendTo($(this));
            for (var i = 0; i < minPerSlide; i++) {
                next = next.next();
                if (!next.length) {
                    next = $(this).siblings(':first');
                }
                next.children(':first-child').clone().appendTo($(this));
            }
        });
    </script>


    @*<div class="r-gallery-sec">
            <div class="r-main-heading">
                <span>Photo Gallery </span>

            </div>

            <div class="r-gallery-body">
                <div class="row">
                    <div id="r-gallery-carousel" class="carousel slide w-100" data-ride="carousel">
                        <div class="carousel-inner w-100" role="listbox">
                            @{ int i = 0;}
                            @foreach (var item in Model)
                            {
                                if (i == 0)
                                {<div class="carousel-item active">
                                        <div class="col-md-3">
                                            <img src="@item.pictureurl" class="img-thumbnail" alt="@item.albumid">
                                        </div>
                                    </div>}
                                else
                                { <div class="carousel-item">
                                        <div class="col-md-3">
                                            <img src="@item.pictureurl" class="img-thumbnail" alt="@item.albumid">
                                        </div>
                                    </div>
                                }
                                i = i + 1;

                            }
                        </div>
                    </div>
                </div>
                <div class="r-gallery-controls mt-3 text-right">
                    <a href="#r-gallery-carousel" class="btn btn-secondary" data-slide="prev"><i class="fa fa-chevron-left"></i></a>
                    <a href="#r-gallery-carousel" class="btn btn-secondary" data-slide="next"><i class="fa fa-chevron-right"></i></a>
                </div>
            </div>
        </div>

        <script src="js/scripts.js"></script>
        <script type="text/javascript">
            $('#myCarousel').carousel({
                interval: 1000
            })
            $('.carousel .carousel-item').each(function () {
                var minPerSlide = 4;
                var next = $(this).next();
                if (!next.length) {
                    next = $(this).siblings(':first');
                }
                next.children(':first-child').clone().appendTo($(this));
                for (var i = 0; i < minPerSlide; i++) {
                    next = next.next();
                    if (!next.length) {
                        next = $(this).siblings(':first');
                    }
                    next.children(':first-child').clone().appendTo($(this));
                }
            });
        </script>*@
